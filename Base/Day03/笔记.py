# 0 = False
# 其他数字都是True
#
# 循环体
#
# 三种执行方法
# 顺序执行、选择执行、循环执行
# 先准备数据再进行逻辑
# 严禁一步到位
#
# while 循环和 while 循环嵌套
# 循环内使用：break打断循环、continue跳过一轮循环，立即开始下一轮循环的条件判断
# 内外循环不能使用同一个变量
# 内循环的变量必须放在外循环里/内循环前面初始化
#
# print的输出换行\n可以修改，修改使用end=''
#
# 占位符加数字%d>>%2d
#
# for 循环
# 遍历的概念
# for和while 循环里的else 只在循环正常结束时执行，被打断不会被执行。
# continue不影响循环正常使用，也就是会正常显示else。
#
# 字符串的概念
# 单引双引和转义>>>三引：自动转义
# 空格也是字符，会被输出
#
# ACCOUNT PASSWORD
#
# 下标和切片
# 下标不可越界
# 切片正数负数都是：前含后不含、起左完右。[起始:结束]
# 切片无越界-
#
# len()获取字符串长度
#
